# Copy to .env.local and fill in to enable the Gemini-backed assistant (ROADMAP item 1).
# Never commit .env.local. Note: a key shipped to the browser is visible to users —
# fine for a personal demo with a restricted key; mention this trade-off at interview.
VITE_GEMINI_API_KEY=
